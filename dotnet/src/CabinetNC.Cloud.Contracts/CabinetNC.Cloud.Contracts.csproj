<Project Sdk="Microsoft.NET.Sdk">

  <!-- Desktop <-> intranet API wire contracts only. Deliberately references nothing else so the
       customer build (Task 12) can ship it without dragging the compute engine along. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
