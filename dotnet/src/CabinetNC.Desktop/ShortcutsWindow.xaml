<Window x:Class="CabinetNC.Desktop.ShortcutsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="快捷键"
        Width="560" SizeToContent="Height" MaxHeight="720"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource WindowBrush}" FontFamily="Segoe UI" FontSize="13"
        ShowInTaskbar="False">
  <DockPanel Margin="20">
    <DockPanel DockPanel.Dock="Bottom" Margin="0,14,0,0">
      <Button DockPanel.Dock="Right" Content="关闭" IsCancel="True" Padding="16,6" />
      <TextBlock Text="视口与密排快捷键在画布有焦点时生效；输入框内不触发。" Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
    </DockPanel>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
      <ItemsControl x:Name="Groups">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <StackPanel Margin="0,0,0,12">
              <TextBlock Text="{Binding Key}" Style="{StaticResource FieldLabel}" />
              <ItemsControl ItemsSource="{Binding}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Grid Margin="0,2">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="190" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <Border Background="{StaticResource HoverBrush}" CornerRadius="4" Padding="8,2" HorizontalAlignment="Left">
                        <TextBlock Text="{Binding Keys}" FontFamily="Consolas" FontSize="12" />
                      </Border>
                      <TextBlock Grid.Column="1" Text="{Binding Action}" VerticalAlignment="Center" Margin="10,0,0,0" TextWrapping="Wrap" />
                    </Grid>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </DockPanel>
</Window>
